import React from 'react';

require('./Game.css');

export default class Game extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <svg></svg>
    );
  }
}
